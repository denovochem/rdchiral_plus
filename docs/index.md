Install rdchiral_mypyc with pip:

```shell
pip install rdchiral_mypyc
```

Alternatively, pip install directly from the repo:

```shell
pip install git+https://github.com/denovochem/rdchiral_mypyc.git
```