Install rdchiral_plus with pip:

```shell
pip install rdchiral_plus
```

Alternatively, pip install directly from the repo:

```shell
pip install git+https://github.com/denovochem/rdchiral_plus.git
```