name: Deploy Docs

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: 3.x
      
      - name: Install dependencies
        run: |
          pip install mkdocs mkdocstrings[python] mkdocs-material
          RDCHIRAL_USE_MYPYC=0 pip install -e .  # Install pure-Python package for docs introspection
      
      - name: Deploy docs
        run: mkdocs gh-deploy --force
